import 'package:flutter/material.dart';

import '../../../social/post/create_post_screen.dart';

/// Tab 3 — Create Post. Hosts the image-post composer.
class TabCreatePost extends StatelessWidget {
  const TabCreatePost({super.key});

  @override
  Widget build(BuildContext context) => const CreatePostScreen();
}
